{%- comment -%}
    add {% render 'random-number' %}
    {{ random_number }} -> here's your random number
{%- endcomment -%}

{%- assign min = 0 -%}
{%- assign max = 9999999 -%}
{%- assign diff = max | minus: min -%}
{%- assign random_number = "now" | date: "%N" | modulo: diff | plus: min -%}